<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.team.mapper.BookmarkMapper">
	<select id="bookmarkL" resultType="com.example.team.dto.BookmarkVO">
		select * from bookmark where b_id = #{b_id}
	</select>
	
	<insert id="bookmarkR">
		insert into 
			bookmark (b_id, b_name, b_imgurl)
		values
			(#{b_id}, #{b_name}, #{b_imgurl})
	</insert>
	
	<delete id="bookmarkD">
		delete from bookmark where b_id = #{b_id} and b_name = #{b_name}
	</delete>
	
	<select id="bookmarkC">
		select count(*) from bookmark where b_id = #{b_id} and b_name = #{b_name}
	</select>
	
</mapper>