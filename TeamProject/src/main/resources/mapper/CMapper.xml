<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.team.mapper.CMapper">
	
	<select id="getList" resultType="com.example.team.dto.Cvo">
		select * from comment where c_getnum = #{c_getnum}
	</select>
	
	<insert id="cRegister">
		insert into 
			comment (c_id , c_getnum , c_content , c_registerdate , c_modifydate) 
		values
			(#{c_id} , #{c_getnum} , #{c_content} , #{c_registerdate} , #{c_modifydate}) 
	</insert>
		
	<select id="cGet">
		select * from comment where c_num = #{c_num}
	</select>
	
	<update id="cUpdate">
		update 
			comment
		set
			c_content = #{c_content}, c_modifydate = #{c_modifydate} 
		where
			c_num = #{c_num}
	</update>
	
	<delete id="cDelete">
		delete from comment where c_num = #{c_num}
	</delete>
	
	<select id="getCount">
		select count(*) from comment where c_getnum = #{c_getnum}
	</select>
	
</mapper>
        